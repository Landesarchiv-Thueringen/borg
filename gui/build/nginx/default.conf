server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    location / {
        root   /app;
        index  index.html;
        try_files $uri$args $uri$args/ /index.html;
    }
}